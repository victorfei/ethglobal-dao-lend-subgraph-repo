type Bond @entity {
  id: ID!
  name: String! 
  symbol: String!
  maturity: BigInt!
  amount: BigInt!
  paymentToken: Token
  dao: Dao
}

type Dao @entity {
  id: ID!
  name: String!
}

type Token @entity {
  id: ID!
  name: String
  symbol: String
}
